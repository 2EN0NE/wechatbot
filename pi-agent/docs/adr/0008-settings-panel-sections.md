# 设置面板二级分区（原生 / 透传 / 设置）

`/wechat-setting` 面板采用两级 tab：一级为作用域（项目级 / 用户级，Tab 切换），二级为分区（原生 → 透传 → 设置，←/→ 切换）。原生区放全部 6 条原生命令（豁免命令灰化锁定），透传区放插件命令，设置区放非命令配置（当前仅不活跃超时）。

选择三分区而非扁平清单，是因为扁平清单把「超时设置、豁免恒开的原生命令、默认关闭的透传命令」混在一起，用户难以分辨哪些能在微信端直接输入。设置区放最后，因为它是低频配置、不是命令。
