# /help 面向用户 + 双通道发现

`/help` 是面向微信用户的精简说明：不暴露平台层 / 桥接层 / 使用层的内部结构，只列影响使用的限制，并动态注入生效中的不活跃超时值（项目级 > 用户级 > 默认）；命令列表只列当前已启用的命令、原生在前、透传在后。`help` 默认开、`status` 默认关。

因微信 iLink 无法在连接时主动推消息（需 context_token），发现走双通道：连接后在 pi TUI 显示一次 `/help`，以及每次（重）连接后对某用户首条消息回发欢迎语（同一连接期内每用户一次）。
